Workflow: 
Creating a mutation or query
    -Select the graphql folder
    -Add the query or mutation that we want to create
    -run yarn gen
    -gen will generate the hook
    -Then we use the custom hook