query Goals($limit: Int!, $cursor: String) {
  goals(limit: $limit, cursor: $cursor) {
    hasMore
    goals {
      id
      monthGoal
      monthGoalString
      createdAt
      updatedAt
      date
    }
  }
}
